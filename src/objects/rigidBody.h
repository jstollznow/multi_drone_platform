#include <string>
class rigidBody
{
    private:
        // platformID
        int platID;
        //rigid body tag
        std::string rigidBody;
    

    // std::vector<rigidBody*> arr;
    // rigidBody* body = arr[0];
    // if (body->isDrone()) {
    //     drone* d = (drone*)body;
    // }
};